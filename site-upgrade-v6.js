(() => { const root=document.querySelector('#landing'); if(!root)return; const nav=document.querySelector('.nav-links'); const link=document.createElement('a'); link.href='#insights'; link.textContent='Insights'; if(nav && ![...nav.children].some(x=>x.textContent.trim()==='Insights')) nav.appendChild(link); const cards=[...document.querySelectorAll('.insight-card')]; cards.forEach(card=>card.addEventListener('click',e=>{if(e.target.tagName==='A')return; card.classList.toggle('insight-active')})); })();
